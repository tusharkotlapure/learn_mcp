[project]
name = "test-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Tushar Kotlapure", email = "t.kotlapure@serrala.com" }
]
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=2.1.1",
    "openai>=3.8.0",
    "pypdf>=6.18.1",
    "python-docx>=1.2.0",
]

[project.scripts]
test-mcp = "test_mcp:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
